# Core
numpy>=1.21
scipy>=1.7
pandas>=1.3

# Neuroimaging
nibabel>=4.0
nilearn>=0.10

# Statistics — PRIMARY: pymer4 wraps R lme4 (gold standard)
# Requires R >= 4.0 with lme4 and lmerTest packages installed.
# Install R packages: R -e 'install.packages(c("lme4", "lmerTest", "Matrix"))'
pymer4>=0.8.1
statsmodels>=0.13  # fallback if R not available

# Parallel processing
joblib>=1.1
tqdm>=4.62

# Plotting
matplotlib>=3.5
seaborn>=0.12

# Optional but recommended
scikit-learn>=1.0
psutil>=5.8
